// Logical Operators Example
let isAdult = true;
let hasID = false;

console.log(isAdult && hasID); // Output: false (both must be true)
console.log(isAdult || hasID); // Output: true (one is true)
console.log(!isAdult); // Output: false (negation)
